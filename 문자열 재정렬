data = input()
result = []
value = 0

for x in data:
  if x.isalpha():
    result.append(x)
  else:
    value += int(x)

result.sort()
if value != 0:
  result.append(str(value))
print(''.join(result))

# 먼저 알파벳의 경우 리스트에 먼저 저장한 뒤 sort,
# 숫자의 경우 더한 뒤 맨뒤에 append.
# isalpha라는 함수를 처음봤다.
