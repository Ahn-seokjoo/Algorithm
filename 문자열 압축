def solution(s):
    answer = len(s)
    for step in range(1,len(s) //2 + 1):
        compressed = ""
        prev = s[0:step]
        count = 1
        #step씩 증가하여 이전 문자열과 비교
        for j in range(step,len(s),step):
            #이전 상태와 같다면 압축 횟수(count) 증가
            if prev == s[j:j + step]:
                count += 1
            else:
                compressed += str(count) + prev if count >= 2 else prev
                prev = s[j:j + step] #다시 상태 초기화
                count = 1
        #남아 있는 문자열 처리
        compressed += str(count) + prev if count >=2 else prev
        #만들어지는 압축 문자열이 가장 짧은 것이 정답
        answer = min(answer,len(compressed))
    return answer
            
#2019 카카오 블라인드 채용 문제, 문제들중 가장 쉬운편인데 어려움.. 잘 볼 것
